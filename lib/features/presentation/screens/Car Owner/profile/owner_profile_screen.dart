import 'package:flutter/material.dart';

class OwnerProfileScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Owner Profile', style: Theme.of(context).textTheme.headlineMedium),
    );
  }
}
